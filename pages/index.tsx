import type { NextPage } from "next";
import { Button } from "@mantine/core";

const Home: NextPage = () => {
  return (
    <div>
      <Button>ckick</Button>
    </div>
  );
};

export default Home;
